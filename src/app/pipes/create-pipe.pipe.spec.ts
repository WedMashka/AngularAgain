import { CreatePipePipe } from './create-pipe.pipe';

describe('CreatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CreatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
